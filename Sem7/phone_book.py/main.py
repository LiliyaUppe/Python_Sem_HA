import html_generate as ht
import xml_generate as xg
import data_provider as dp
import logger as log
# print(hc.create())
# print(xg.create())
0
log.name_phone_logger(ht.new_create(xg.new_create(dp.data_collection())))
